# DOC-ENV allowlist

Each entry is an SDK env var the DOC-ENV gate flags but is a genuinely-justified
non-finding. Format: `- <VAR_NAME> — reason (approver, date)`.

(No entries — every SDK env var read by the port is documented.)
